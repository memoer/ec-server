import { AddCategoryFilter } from '~/_lib/filter/add-category.filter';

describe('AddCategoryFilter', () => {
  it('should be defined', () => {
    expect(new AddCategoryFilter()).toBeDefined();
  });
});

{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node-terminal",
      "name": "Run Script: start:local:server",
      "request": "launch",
      "command": "npm run start:local:server",
      "cwd": "${workspaceFolder}"
    }
  ]
}